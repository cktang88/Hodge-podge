# mongo config file
# usage: "mongod --config mongo.config" OR "mongod -f mongo.config"
# this is in YAML file format (spaces, not tabs!)

##store data here
dbpath=./mongodb/data

##all output go here
logpath=./mongodb/log/mongo.log